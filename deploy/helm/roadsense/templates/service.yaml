apiVersion: v1
kind: Service
metadata:
  name: roadsense-api
spec:
  type: {{ .Values.service.type }}
  selector:
    app: roadsense-api
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8000
